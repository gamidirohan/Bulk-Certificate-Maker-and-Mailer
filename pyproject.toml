[project]
name = "certificate-generator"
version = "0.1.0"
description = "Certificate generation and delivery utility"
requires-python = ">=3.11"
dependencies = [
  "Flask>=3.0",
  "Pillow>=10.0",
  "openpyxl>=3.1",
  "python-dotenv>=1.0",
  "requests>=2.31",
]

[tool.uv]
package = false
